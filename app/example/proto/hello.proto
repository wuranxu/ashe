syntax = "proto3";

import "ashe/protocol/protocol.proto";


service HelloService {
    rpc Hello (protocol.Request) returns (protocol.Response) {
    };
    rpc Assert (protocol.Request) returns (protocol.Response) {
    };
}