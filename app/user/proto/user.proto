syntax = "proto3";

import "ashe/protocol/protocol.proto";

option go_package = "ashe/app/user/proto";


service user {
    // 注册用户
    rpc Register (protocol.Request) returns (protocol.Response) {
    };
    rpc Login (protocol.Request) returns (protocol.Response) {
    };
    rpc Edit (protocol.Request) returns (protocol.Response) {
    }
    rpc InsertUserLog (protocol.Request) returns (protocol.Response) {
    }
}